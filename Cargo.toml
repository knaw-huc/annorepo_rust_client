[package]
name = "annorepo_client"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12.12", features = ["gzip", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
